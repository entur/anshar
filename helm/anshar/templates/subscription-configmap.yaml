
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "app.name" . }}-subscription-config
  namespace: {{ .Release.Namespace }}
  labels:
  {{- include "common.labels" . | indent 4 }}
data:
  subscriptions.yml: |-{{ range .Files.Lines .Values.subscriptions}}
    {{ . }}{{ end }}


